---
- name: "restart solr linux"
  service:
    name: "{{ solr_service_name }}"
    state: restarted
    enabled: True
  when: solr_with_systemd
  become: True

- name: "restart solr windows"
  win_service:
    name: "{{ solr_service_name }}"
    state: restarted
